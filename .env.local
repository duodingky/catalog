PORT=3000
DATABASE_URL=postgres://postgres:postgres@localhost:5432/ecom

# RSA public key used to verify Bearer JWTs (RS256).
# You can provide a PEM string (with \n) or a base64-encoded PEM.
JWT_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----

